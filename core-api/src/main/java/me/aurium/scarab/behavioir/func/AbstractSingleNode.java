package me.aurium.scarab.behavioir.func;

import me.aurium.scarab.behavioir.Behavior;
import me.aurium.scarab.behavioir.BehaviorComposite;
import me.aurium.scarab.behavioir.BehaviorNode;
import me.aurium.scarab.behavioir.BehaviorResult;

/*
public class AbstractSingleNode<T extends Behavior,C extends BehaviorResult<T>> implements BehaviorNode<T,C> {

    private final ActionFunction<T,C> resultifier;
    private final BehaviorNode<T,BehaviorResult<T>> childNode;


    public AbstractSingleNode(ActionFunction<T, C> resultifier, BehaviorNode<T, BehaviorResult<T>> childNode) {
        this.resultifier = resultifier;
        this.childNode = childNode;
    }


    @Override
    public <X extends BehaviorResult<T>> X consume(BehaviorComposite<T, C> composite) {



        return childNode.consume(composite);
    }
}
*/
